# Option 1: OpenAI (better quality)
OPENAI_API_KEY=sk-your-key-here

# Option 2: Free HuggingFace (no key needed, uses local models)
# Just leave OPENAI_API_KEY empty and it will use HuggingFace

# Model settings
EMBEDDING_MODEL=all-MiniLM-L6-v2
CHUNK_SIZE=500
CHUNK_OVERLAP=50
